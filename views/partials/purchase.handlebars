<h1>Checkout</h1>

<div class="row">
    <div class="col-md-12">
        <ul>
            {{#each saleItems}}
            <li>
                <p class="saleItems">{{id}}. {{SaleItems}}</p>
            </li>
            {{/each}}
        </ul>
    </div>
</div>