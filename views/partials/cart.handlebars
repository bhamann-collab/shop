<h1>Your Cart</h1>

<div class="row">
    <div class="col-md-12">
        <ul>
            {{#each orders}}
            <li>
                <p class="orders">{{id}}. {{Orders}}</p>
            </li>
            {{/each}}
        </ul>
    </div>
</div>