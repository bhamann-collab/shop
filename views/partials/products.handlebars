<h1>Our Products</h1>4

<div class="row">
    <div class="col-md-12">
        <ul>
            {{#each items}}
            <li>
                <p class="items">{{id}}. {{Items}}</p>
            </li>
            {{/each}}
        </ul>
    </div>
</div>